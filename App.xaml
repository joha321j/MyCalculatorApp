<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyCalculatorApp.App">
    <Application.Resources>
        <ResourceDictionary>
            <Style x:Key="StandardButton" TargetType="Button">
                <Setter Property="CornerRadius" Value="0"/>
                <Setter Property="FontSize" Value="36"/>
            </Style>
            <Style x:Key="NumberButton" TargetType="Button" BasedOn="{StaticResource StandardButton}">
                <Setter Property="BackgroundColor" Value="White"/>
                <Setter Property="TextColor" Value="Black"/>
            </Style>
            <Style x:Key="OperatorButton" TargetType="Button" BasedOn="{StaticResource StandardButton}">
                <Setter Property="BackgroundColor" Value="#FFA400"/>
                <Setter Property="TextColor" Value="White"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>